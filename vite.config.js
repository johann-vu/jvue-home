import { defineConfig } from "vite";

export default defineConfig({
  // Custom domain deployment on GitHub Pages. If you later deploy to
  // <user>.github.io/<repo>/, change this to "/<repo>/".
  base: "/",
});
